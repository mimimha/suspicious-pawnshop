/**
 * 기획 밸런스 값.
 *
 * 여기 있는 값은 확정이 아니라 **현재 테스트값**이다. 플레이 테스트로 조정한다.
 * 로직 파일에 숫자를 직접 쓰지 않고 반드시 이 모듈을 통해 참조한다.
 *
 * 출처: Notion `전당포 게임 > 통합 기획서` §4(DAY 기본 규칙), §32(시간 처리)
 * 스냅샷: 2026-08-16
 */
export interface GameBalance {
  /** STAGE 하나를 구성하는 DAY 수. */
  daysPerStage: number;
  /** 하루 기본 영업시간(초). */
  dayDurationSeconds: number;
  /** 게임 시작 시 보유 현금(원). */
  startingMoney: number;
  /** STAGE 번호(1부터 시작)에 대응하는 월세. */
  stageRents: readonly number[];
  /**
   * STAGE를 넘기기 위해 그 STAGE 동안 확정해야 하는 최소 실현 손익(원).
   * 0이면 목표 없음. 월세만으로는 아무 거래도 하지 않아도 통과되는 STAGE를 막는다.
   */
  stageProfitGoals: readonly number[];
  /** 데모에서 플레이 가능한 마지막 STAGE. */
  finalDemoStage: number;
}

export const GAME_BALANCE: GameBalance = {
  daysPerStage: 3,
  dayDurationSeconds: 180,
  startingMoney: 55_000,
  // 월세는 STAGE 종료 시 현금에서 빠지고, 남은 현금이 다음 STAGE 운영자금이 된다.
  // 두 월세 합(57,000)이 시작 자금(55,000)보다 커야 아무 거래도 하지 않고 데모를 통과할 수 없다.
  //
  // 사용자 승인 변경(2026-08-26): 24,000 / 40,000 에서 낮췄다. 완주율을 올리기 위한 조정이다.
  // 다만 월세는 완주율에 대한 지레가 약하다(단독으로 내리면 '보통' 플레이 완주율이 1pp만 오른다).
  // 실제 병목은 가품 손실이었고 그쪽을 함께 완화했다(authenticityConfig 참고).
  stageRents: [22_000, 35_000],
  // 월세만으로는 무거래 통과가 가능한 STAGE를 최소 실현 손익으로 막는다.
  //
  // 사용자 승인 변경(2026-08-26): STAGE 2에 목표를 넣고(0 → 6,000) STAGE 1은 3,000 → 2,000으로 낮췄다.
  // STAGE 2에 목표가 없으면 STAGE 1에서 크게 벌어 둔 플레이어가 마지막 STAGE에서 아무 거래도
  // 하지 않고 이월 자금으로 월세만 내고 완주할 수 있었다. 마지막 STAGE가 데모의 절정인데
  // 거래가 필요 없었다.
  //
  // 값은 통과율 시뮬레이션(3,000판 x 2스테이지 연속, 최저 수용가를 모르는 플레이어를 모사해
  // 최초 제시가의 일정 비율부터 3라운드까지 흥정하고 결렬·공격·병원비·재고 9칸·현금 제약을
  // 모두 반영)으로 잡았다. 최초 제시가의 75%부터 부르는 '보통' 플레이가 69%,
  // 65%부터 부르는 '공격적' 플레이가 91% 완주한다.
  stageProfitGoals: [2_000, 6_000],
  finalDemoStage: 2,
};
