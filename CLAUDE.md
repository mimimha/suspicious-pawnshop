# vive-pawnshop

## 프로젝트 개요

플레이어가 전당포를 운영하며 물건을 감정·흥정·매입하고 시세에 맞춰 판매하는 **경영 게임**.
현재는 **AI 개발 handoff 기반 구조만** 구축된 상태이며, 게임 구현은 시작하지 않았다.

## 프로젝트 목표

- **OpenAI Game Builders Seoul 출품용 데모 게임**이다.
- 구현 범위는 **STAGE 1~2까지**. 그 이후 STAGE는 데모 범위 밖이다.
- 총 플레이타임 **10분 이내**를 목표로 한다.
- 목표는 상용 규모의 확장이 아니라, **AI를 활용해 짧은 기간 안에 빠르고 재미있는
  완결된 데모를 만드는 것**이다.
- 따라서 **과도한 추상화보다 변경 용이성과 개발 속도를 우선**한다.
  다만 데모라는 이유로 이후 기획 변경을 막는 임시 하드코딩을 쌓지는 않는다.

## 기술 스택

- **게임 엔진: Phaser 4.2.1 (확정)**
- 언어: TypeScript / 번들러: Vite / 패키지 매니저: npm
- 기준 해상도: **1280×720 (16:9)**, `Phaser.Scale.FIT` + `CENTER_BOTH`

Phaser는 이 프로젝트의 확정 엔진이다. Phaser 3 호환을 위한 다운그레이드는 하지 않는다.

### 상태 관리

- 하나의 거대한 전역 Store로 모든 데이터를 관리하지 **않는다.**
- 최상위 `GameState`는 게임 전체 흐름에 필요한 **최소 상태만** 소유한다
  (Stage / Day / 현재 진행 Phase / Money).
- `Inventory`, `Market`, `Customer` 등은 **각 시스템이 자신의 상태와 책임을 소유**한다.
- 시스템 간 직접 의존을 최소화하고, 필요한 데이터만 명확한 인터페이스로 주고받는다.
- Redux 등 별도 상태 관리 라이브러리는 도입하지 않는다.

### 밸런스 데이터

- 기획값을 게임 로직 내부에 직접 하드코딩하지 않는다.
- Day 수, 영업시간, 시작 자금, 월세, 재고 크기 등 변경 가능성이 높은 값은
  `src/config/`의 **TypeScript config/constant 모듈**에서 관리한다.
- 초기에는 JSON이 아니라 TS 모듈을 쓴다. 데이터 규모가 커지면 JSON 등
  외부 데이터 형식으로 분리할 수 있도록 구조를 열어 둔다.

## 개발 워크플로

```
ChatGPT (기획 / PRD / 의사결정)
  → .handoff/task.md (작업 지시)
  → Claude Code (구현 → 검증 → 실패 분석 → 수정 → 재검증)
  → .handoff/result.md + state.json (결과 기록)
  → 결정 필요 시에만 .handoff/decision.md 작성 후 중단
  → .handoff/history/ (완료 기록 보관)
```

장기 목표는 `ChatGPT → Local Bridge → Claude Code` 자동화이며,
현재 단계는 그 기반(handoff protocol + 상태 관리)만 갖춘다.
ChatGPT API 연동, 로컬 브리지, 웹 서버는 아직 **직접 만들지 않는다.**
(공식 원격 MCP 서버에 연결해 조회하는 것은 여기에 해당하지 않는다. 아래 참조.)

## 디렉터리 구조

| 경로 | 역할 |
| --- | --- |
| `CLAUDE.md` | Claude Code가 항상 참고하는 개발 규칙 |
| `docs/prd/step-NN/` | Step별 구현용 PRD 보관 (Notion의 Step / PRD 체계와 일치) |
| `.handoff/task.md` | 외부에서 전달하는 현재 작업 지시 |
| `.handoff/result.md` | 직전 작업의 수행 내용과 결과 |
| `.handoff/state.json` | 기계가 읽는 현재 작업 상태 |
| `.handoff/decision.md` | 사용자 결정이 필요할 때의 질문서 |
| `.handoff/history/` | 완료된 handoff 기록 보관 |
| `.claude/skills/handoff-loop/` | handoff 작업 절차 (자율 반복 / 중단 기준) |
| `.claude/skills/playtest-loop/` | 플레이 감각 확인이 필요한 작업의 사용자 검증 절차 |
| `scripts/claude-handoff.ps1` | task.md를 Claude Code CLI에 전달하는 실행 스크립트 |

## 기획 원본과 PRD

기획 원본은 **Notion**에 있고, repo의 `docs/prd/`는 **구현 시점의 확정 스냅샷**이다.

| | 위치 | 성격 |
| --- | --- | --- |
| 원본 기획 | Notion `🦊 전당포 게임` > `통합 기획서` ([링크](https://app.notion.com/p/3be135971f3c80118525db446b78f0b2)) | Source of Truth. 계속 바뀔 수 있다. |
| 개발 규칙 원본 | Notion `🦊 전당포 게임` > `📏 Codex/Claude 에게 알려줘야 할 프롬프트` ([링크](https://app.notion.com/p/3bd135971f3c80669e11ddb03b057a23)) | 게임 개발 구조·PRD 순서 원칙 |
| 구현용 PRD | `docs/prd/step-NN/prd-N-M-*.md` | 해당 구현 시점에 확정된 implementation snapshot |

규칙:

- Notion은 **조회 전용**으로 사용한다. 페이지를 생성·수정·삭제·이동하지 않는다.
  기획 변경이 필요하다고 판단되면 직접 고치지 말고 `decision.md`로 사용자에게 올린다.
  이 원칙은 `.claude/settings.json`의 `permissions.deny`로도 강제한다 (write 계열 MCP 툴 차단).
- 구현할 때 Notion 전체를 매번 읽지 않는다. 필요한 부분만 확인한 뒤
  `docs/prd/`에 구현용 PRD로 정리하고, **이후 구현은 그 PRD를 기준으로 한다.**
- PRD와 Notion 원본이 어긋나 보이면 임의로 판단하지 말고 사용자에게 확인한다
  (`waiting_for_decision`). PRD 스냅샷은 실행 중에 조용히 바뀌지 않는다.
- PRD 문서에는 출처(Notion 페이지 제목)와 스냅샷 시점을 남긴다.
- **밸런스 미확정 값은 지금 확정하지 않는다.** Notion에 "테스트값" 또는 미확정으로
  적힌 수치(예: STAGE 2 월세, 결렬·공격 확률, 신뢰도 증감량, 병원비)는 PRD에도
  **"현재 테스트값"으로 명시해서 가져오고**, 플레이 테스트를 통해 조정한다.
  이런 값은 코드에 고정하지 않고 데이터/설정으로 분리한다.

### 사용자 승인 기획 변경 기록

- 사용자가 기존 기획, 밸런스, 플레이 흐름 또는 UX 규칙을 변경하면 코드 수정 전에 관련
  PRD와 `docs/design-change-log.md`를 함께 갱신한다.
- 변경 이력에는 날짜, 변경 전·후, 이유, 영향 범위, 테스트값/확정값 상태를 남긴다.
- Notion 원본과 다른 사용자 승인 사항은 `사용자 승인 변경`으로 표시하고 Notion은 수정하지 않는다.
- 단순 버그 수정, 내부 리팩터링, 오탈자처럼 게임 규칙을 바꾸지 않는 작업은 기록하지 않는다.
- Codex는 루트 `AGENTS.md`, Claude는 이 문서를 통해 같은 기록 규칙을 적용한다.

## 작업 절차

handoff 작업을 수행할 때는 **`handoff-loop` skill의 절차를 따른다.**
요약하면:

- 현재 승인된 PRD 범위 안에서는 구현 → 검증 → 수정 → 재검증을 스스로 반복한다.
- 파일명·함수명·내부 구현·작은 리팩터링·버그 수정은 스스로 결정한다.
- 기획 변경, UX 영향, 요구사항 충돌, PRD 범위 초과, 구조적 분기,
  데이터 손실/대규모 리팩터링 위험은 **멈추고 `decision.md`를 작성한다.**
- `state.json`의 `maxIterations`를 넘기면 중단한다. 무한 반복하지 않는다.
- 현재 PRD가 완료되면 다음 PRD를 임의로 시작하지 않는다.
- **실제 플레이 감각(UI 배치, 템포, 조작감, 시각 피드백, 재미)에 영향을 주는 변경은
  자동 검증 통과만으로 `completed` 처리하지 않는다.** `ready_for_playtest`로 멈추고
  사용자 확인을 받는다 — `playtest-loop` skill의 절차를 따른다.

## 현재 단계의 제약

아래 항목은 **명시적인 지시가 있을 때까지 수행하지 않는다.**

1. 승인된 PRD 범위를 벗어난 게임 기능을 구현하지 않는다.
2. 상세 PRD를 임의로 작성하지 않는다.
3. `vive-pawnshop` 프로젝트 디렉터리 밖의 파일을 수정하지 않는다.
4. 외부 명령을 임의로 실행하는 범용 서버/브리지를 만들지 않는다.
5. 구조를 필요 이상으로 복잡하게 만들지 않는다.
6. 데모 규모를 넘어서는 추상화, 지금 쓰지 않는 인터페이스/계층/패턴을 미리 만들지 않는다.

## 작업 규칙

- 작업 지시는 `.handoff/task.md`를 기준으로 한다. 지시 범위를 임의로 넓히거나 줄이지 않는다.
- 작업 완료 후 `.handoff/result.md`와 `.handoff/state.json`을 반드시 갱신한다.
  형식은 각 파일의 템플릿과 `handoff-loop` skill을 따른다.
- 검증은 실제로 실행 가능한 방법(테스트, 실행, 타입 체크)으로 한다.
  코드를 읽어서 맞아 보인다는 것은 검증이 아니다.
- 되돌리기 어려운 작업(파일 대량 삭제, 외부 전송, 설치)은 지시에 명시된 경우에만 수행한다.
- 막혀서 중단할 때도 작업 중인 코드를 되돌리지 않는다. 진행 상황을 기록하고 멈춘다.

## 개발 환경

- OS: Windows 11
- Shell: PowerShell 5.1
- 게임 엔진: Phaser (확정, 미설치)
- 상세 게임 개발 규칙(코딩 컨벤션, 아키텍처, 테스트 정책)은 이후 별도로 이 문서에 추가한다.
  구조 원칙의 원본은 Notion `📏 Codex/Claude 에게 알려줘야 할 프롬프트` 페이지다.
